--- After (Refactored Implementation)
public class AgeUtils {

    /**
     * Calculates the average age from a list of ages.
     * Returns 0.0 if the list is empty.
     */
    public double calculateAverageAge(List<Integer> ages) {
        if (ages == null || ages.isEmpty()) {
            return 0.0;
        }

        int total = 0;
        for (int age : ages) {
            total += age;
        }

        double averageAge = (double) total / ages.size();
        return averageAge;
    }
}

--- Before (Old Implementation)
public class AgeUtils {

    // Misleading name and vague variables
    public double magicNumber(List<Integer> x) {
        int y = 0;
        for (int i : x) {
            y += i;
        }
        return y / x.size();  // ⚠️ risk of division by zero
    }
}